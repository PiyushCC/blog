<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{% if title %}{{ title }} | {% endif %}{{ site.title }}</title>
  <meta name="description" content="{{ description or site.description }}">
  {% if page.url %}<link rel="canonical" href="{{ site.url }}{{ page.url | url }}">{% endif %}
  <meta property="og:title" content="{% if title %}{{ title }} | {% endif %}{{ site.title }}">
  <meta property="og:description" content="{{ description or site.description }}">
  <meta property="og:type" content="website">
  {% if site.url and page.url %}<meta property="og:url" content="{{ site.url }}{{ page.url | url }}">{% endif %}
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=DM+Sans:ital,opsz,wght@0,9..40,400;0,9..40,500;0,9..40,600;0,9..40,700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/main.css">
  <link rel="alternate" type="application/atom+xml" title="{{ site.title }} (Atom)" href="/feed/feed.xml">
  {% block head %}{% endblock %}
</head>
<body data-theme="light">
  <div id="search-overlay" class="search-overlay" aria-hidden="true">
    <div class="search-box">
      <div id="search"></div>
    </div>
  </div>
  {% include "navbar.njk" %}
  <main class="main">
    {{ content | safe }}
  </main>
  <link rel="stylesheet" href="/pagefind/pagefind-ui.css">
  <script src="/js/main.js"></script>
  <script src="/pagefind/pagefind-ui.js"></script>
  <script>
    window.addEventListener("DOMContentLoaded", function () {
      if (window.PagefindUI) {
        new PagefindUI({
          element: "#search",
          showSubResults: false,
          showImages: false,
          showEmptyFilters: false,
          autofocus: true
        });
      }
    });
  </script>
  {% block scripts %}{% endblock %}
</body>
</html>
